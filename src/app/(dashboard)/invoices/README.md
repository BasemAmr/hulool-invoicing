# /invoices

- page.tsx           — list with status filter (searchParams)
- new/page.tsx       — multi-line draft builder with live totals
- [id]/page.tsx      — detail: status badge, QR preview, issue action
