# (dashboard) — route group

All authenticated UI lives here. URL prefix stays clean: /companies, /customers, /invoices.
The group layout (layout.tsx, added in Slice 2) provides the RTL Arabic shell + sidebar.
