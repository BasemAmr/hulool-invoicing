# components/forms — client form components

Forms bound to server actions via useActionState.
Owns: pending states, field errors, FormData serialization.
Pure presentation — no data fetching, no business rules.
