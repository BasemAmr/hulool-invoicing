/**
 * PDF rendering infrastructure.
 *
 * - template.ts          — DocumentTemplateConfig defaults + merge
 * - invoice-document.tsx — parametric A4 bilingual layout (Strategy)
 * - react-pdf-renderer.ts — PdfRenderer port implementation
 *
 * Fonts: public/fonts/Amiri-*.ttf (must ship with the deploy image).
 */
